{
  "name": "toy-api-server-cloudflare-worker",
  "description": "A toy API server running on Cloudflare Workers, demonstrating basic CRUD operations with rate limiting and Cloudflare KV storage.",
  "version": "0.1.0",
  "author": "Đăng Khoa <i.am@dangkhoa.dev>",
  "license": "MIT",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "node dev.js",
    "deploy": "node deploy.js",
    "check:stag": "wrangler deploy --dry-run --env staging --config wrangler.template.toml",
    "check:prod": "node deploy.js --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
